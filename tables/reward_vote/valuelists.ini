[user]
__sql__ = "select login, concat(login, coalesce(concat(': ', name), '')) from github_users where length(login) > 0 order by login"

[issue]
__sql__ = "select num, concat('#', num, ' ', title) from issue order by num"
